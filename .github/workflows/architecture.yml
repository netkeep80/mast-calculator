name: Architecture foundation checks

on:
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened, ready_for_review]
  push:
    branches: [main]
  workflow_dispatch:

env:
  GIT_CONFIG_KEY_0: init.defaultBranch
  GIT_CONFIG_VALUE_0: main

permissions:
  contents: read

jobs:
  architecture:
    name: Architecture audit
    runs-on: ubuntu-latest
    timeout-minutes: 15
    concurrency:
      group: architecture-${{ github.workflow }}-${{ github.ref }}
      cancel-in-progress: true
    steps:
      - name: Checkout source
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - name: Simulate fresh merge with latest base
        if: github.event_name == 'pull_request'
        shell: bash
        env:
          BASE_REF: ${{ github.base_ref }}
        run: bash scripts/simulate-fresh-merge.sh
      - name: Use Node.js 24
        uses: actions/setup-node@v6
        with:
          node-version: '24.x'
      - name: Check JavaScript syntax
        run: npm run check
      - name: Typecheck canonical contracts
        run: npm run typecheck
      - name: Build TypeScript runtime core
        run: npm run build:core
      - name: Import emitted application API
        run: node --input-type=module -e "await import('./.build/packages/application/index.js')"
      - name: Test runtime contract validation
        run: npm run test:contracts
      - name: Test architecture audit tooling
        run: npm run test:architecture
      - name: Enforce current architecture baseline
        run: npm run audit:architecture
      - name: Publish human-readable snapshot to job summary
        if: always()
        shell: bash
        run: npm run audit:architecture:report >> "$GITHUB_STEP_SUMMARY"
