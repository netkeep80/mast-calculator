# Mast Calculator

Статическое веб-приложение для параметрического расчёта модульной телекоммуникационной мачты. Оно не требует сервера, сборщика и внешних JavaScript-библиотек: расчёт выполняется непосредственно в браузере.

Опубликованная версия:

**https://netkeep80.github.io/mast-calculator/**

## Прототип 0.3

Реализовано:

- генерация пространственной схемы из одинаковых модулей;
- линейный расчёт идеальной 3D-фермы методом конечных элементов;
- собственный вес стержней и вес цилиндрического слоя льда;
- ветровая нагрузка на стержни с учётом пространственной проекции и увеличения диаметра при обледенении;
- ветровая и весовая нагрузка оборудования;
- огибающая по нескольким направлениям ветра;
- проверка растянутых стержней по текучести;
- упругая проверка сжатых стержней по Эйлеру с ограничением по текучести;
- линейный собственный расчёт общей потери устойчивости по геометрической матрице жёсткости;
- визуализация первой формы общей потери устойчивости;
- контроль невязки решения, локального равновесия каждого узла и глобального равновесия;
- подбор минимального стандартного единого диаметра по прочности, отклонению и общей устойчивости;
- ведомость всех стержней по огибающей ветровых направлений;
- группировка материала по типу, диаметру и длине детали;
- экспорт расчётной ведомости в CSV и воспроизводимого отчёта в JSON;
- автоматические аналитические тесты.

## Расчётная ведомость

Для каждого стержня определяется собственный наиболее опасный ветровой случай. В таблице и CSV выводятся:

- узлы и геометрический тип стержня;
- длина и диаметр;
- растяжение или сжатие;
- определяющее продольное усилие;
- максимальные растягивающее и сжимающее усилия по всем направлениям ветра;
- напряжение, расчётная несущая способность, гибкость и коэффициент использования.

JSON-отчёт содержит исходные параметры, сводные результаты, численную диагностику, материальную ведомость, огибающую стержней и предупреждения текущей модели. Он предназначен для воспроизводимости расчёта и последующего сравнения с независимыми КЭ-комплексами.

## CI/CD

Workflow `Pull request checks` запускается для каждого открытия и обновления pull request в `main`. Он выполняет:

1. модульные и аналитические тесты `npm test`;
2. синтаксическую проверку JavaScript `npm run check`;
3. запуск локального HTTP-сервера и проверку доступности статических ресурсов.

Во время недоступности GitHub-hosted runners сайт публикуется из корня ветки `gh-pages`. После восстановления GitHub Actions можно вернуть автоматическую публикацию workflow `Deploy GitHub Pages`.

## Расчёт общей устойчивости

После линейного статического расчёта по найденным продольным усилиям собирается геометрическая матрица жёсткости `K_G`. Критический множитель нагрузки определяется из задачи

```text
(K + λ K_G) φ = 0.
```

Для численного решения используется преобразование через разложение Холецкого упругой матрицы `K`, после чего вычисляется максимальное собственное значение симметричной преобразованной матрицы. В интерфейсе можно показать соответствующую нормированную форму потери устойчивости.

## Важное ограничение

Текущая версия является проверяемым физическим прототипом, а не готовым нормативным заключением. Линейный собственный расчёт общей устойчивости не учитывает начальную кривизну, монтажные отклонения и геометрическую нелинейность. Пока также не учитываются конечная жёсткость и эксцентриситет узлов, болты, резьба, сварные швы, фундамент и усталость.

Топология одного модуля принята как горизонтальный равносторонний треугольник и шесть диагоналей к следующему уровню, повёрнутому на 60°. Эту схему необходимо сверить с фактическими размерами и расположением гаек.

## Локальный запуск

```bash
python3 -m http.server 8080 --directory site
```

После этого открыть `http://localhost:8080`.

Тесты расчётного ядра:

```bash
npm test
npm run check
```

Зависимостей npm у проекта нет; Node.js используется только для автоматических тестов.

## Единицы

Внутри расчётного ядра используются единицы СИ: метры, ньютоны, паскали и килограммы. Интерфейс преобразует миллиметры, гигапаскали и мегапаскали на границе системы.

## Следующие этапы

1. Зафиксировать точную геометрию реального модуля и узлов.
2. Добавить отдельные диаметры для горизонтальных и диагональных стержней.
3. Добавить проверки болтов М24, резьбы, гаек и сварных швов.
4. Добавить геометрически нелинейный расчёт с начальными несовершенствами.
5. Реализовать оптимизацию геометрии, стоимости и раскроя арматуры.
