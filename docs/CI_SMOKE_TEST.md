# CI smoke test

Этот файл создан для проверки, что workflow `Pull request checks`, уже находящийся в ветке `main`, запускается при открытии и обновлении pull request.

Повторный коммит создан для генерации события `pull_request.synchronize` после включения GitHub Actions.
