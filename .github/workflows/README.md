# Workflows

- `ci.yml` — обязательные проверки pull request и push в `main`.
- `pages.yml` — повторная проверка и публикация статического сайта после push в `main`.

Для работы публикации в настройках репозитория должен быть выбран источник GitHub Pages `GitHub Actions`.
